//No carousel is not being used right now.

/* const addClassName = () => {
    const radioButtons = document.querySelectorAll(".radio-input");
    radioButtons[0].setAttribute("checked", "checked");
}

addClassName(); */